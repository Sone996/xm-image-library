export type ImageMetadata = {
  id: string;
  author: string;
  fullUrl: string;
  listUrl: string;
};

export type ImageResponse = {
  id: string;
  author: string;
  width: number;
  height: number;
  url: string;
  download_url: string;
};
